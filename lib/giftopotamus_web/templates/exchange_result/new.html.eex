<h1>New Exchange result</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.exchange_result_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.exchange_result_path(@conn, :index) %></span>
